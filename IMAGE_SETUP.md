# Image setup

Product images are bundled locally under `frontend/public/products/` as lightweight SVG assets. The backend seed data stores these local `/products/...svg` paths, so the catalog does not depend on third-party image hosts at runtime.

The SVG artwork is intentionally clean and product-focused for the assignment demo. Variant color is reflected in the asset filename and artwork.
